<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RehabPlus - Online Appointment Booking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --secondary-gradient: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            --success-gradient: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow-x: hidden;
        }

        /* Hero Section */
        .hero-section {
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            top: -50%;
            left: -50%;
            background:
                radial-gradient(circle at 20% 50%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(255, 255, 255, 0.08) 0%, transparent 50%);
            animation: float 20s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(30px, -30px) rotate(120deg); }
            66% { transform: translate(-20px, 20px) rotate(240deg); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
            color: white;
            text-align: center;
            padding: 40px 20px;
        }

        .hero-content h1 {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }

        .hero-content p {
            font-size: 1.3rem;
            margin-bottom: 40px;
            opacity: 0.95;
        }

        /* Feature Cards */
        .feature-card {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            height: 100%;
            border: 2px solid transparent;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
            border-color: #667eea;
        }

        .feature-icon {
            width: 80px;
            height: 80px;
            background: var(--primary-gradient);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            color: white;
            margin: 0 auto 20px;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
        }

        /* CTA Buttons */
        .btn-hero {
            padding: 18px 50px;
            font-size: 1.2rem;
            border-radius: 50px;
            font-weight: 600;
            border: none;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary-gradient {
            background: white;
            color: #667eea;
        }

        .btn-primary-gradient:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(255, 255, 255, 0.3);
            color: #667eea;
        }

        .btn-outline-light-custom {
            background: transparent;
            border: 3px solid white;
            color: white;
        }

        .btn-outline-light-custom:hover {
            background: white;
            color: #667eea;
            transform: translateY(-3px);
        }

        /* Features Section */
        .features-section {
            padding: 100px 0;
            background: #f8f9fa;
        }

        .section-title {
            font-size: 2.5rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 60px;
            color: #2d3748;
        }

        /* Info Section */
        .info-section {
            padding: 80px 0;
            background: white;
        }

        .info-icon {
            font-size: 3rem;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Footer */
        .footer {
            background: #2d3748;
            color: white;
            padding: 40px 0;
            text-align: center;
        }

        /* Staff Login Link */
        .staff-login {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
        }

        .staff-login a {
            color: white;
            text-decoration: none;
            padding: 10px 25px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: all 0.3s ease;
        }

        .staff-login a:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-content h1 {
                font-size: 2.5rem;
            }
            .hero-content p {
                font-size: 1.1rem;
            }
            .btn-hero {
                padding: 15px 40px;
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Staff Login Link -->
    <div class="staff-login">
        <a href="/login">
            <i class="bi bi-person-circle"></i> Staff Login
        </a>
    </div>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <div class="hero-content">
                <h1><i class="bi bi-heart-pulse"></i> RehabPlus</h1>
                <p>LANTAVAFIX PHTSIOTHERAPY & SPORT INJURY CLINIC</p>
                <p class="mb-5">Book your appointment online - Quick, Easy, No Registration Required</p>

                <div class="d-flex gap-3 justify-content-center flex-wrap">
                    <a href="/book" class="btn btn-hero btn-primary-gradient">
                        <i class="bi bi-calendar-check"></i> Book Appointment Now
                    </a>
                    <a href="#features" class="btn btn-hero btn-outline-light-custom">
                        <i class="bi bi-info-circle"></i> Learn More
                    </a>
                </div>

                <div class="mt-5">
                    <p class="mb-2"><i class="bi bi-clock"></i> Available Time Slots: 9:00 AM - 8:00 PM</p>
                    <p><i class="bi bi-calendar3"></i> Book up to 30 days in advance</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="features-section">
        <div class="container">
            <h2 class="section-title">Why Choose Our Online Booking?</h2>

            <div class="row g-4">
                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="bi bi-lightning-charge"></i>
                        </div>
                        <h3 class="text-center mb-3">Quick & Easy</h3>
                        <p class="text-center text-muted">
                            Book your appointment in just 2 minutes. No registration or complicated forms required.
                        </p>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="bi bi-clock-history"></i>
                        </div>
                        <h3 class="text-center mb-3">Real-Time Availability</h3>
                        <p class="text-center text-muted">
                            See available time slots instantly. Choose the time that works best for your schedule.
                        </p>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="bi bi-shield-check"></i>
                        </div>
                        <h3 class="text-center mb-3">Secure & Private</h3>
                        <p class="text-center text-muted">
                            Your information is protected. View and manage your bookings from any device.
                        </p>
                    </div>
                </div>
            </div>

            <div class="row g-4 mt-4">
                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="bi bi-phone"></i>
                        </div>
                        <h3 class="text-center mb-3">Mobile Friendly</h3>
                        <p class="text-center text-muted">
                            Book from anywhere using your phone, tablet, or computer.
                        </p>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="bi bi-calendar-x"></i>
                        </div>
                        <h3 class="text-center mb-3">Easy Cancellation</h3>
                        <p class="text-center text-muted">
                            Need to cancel? Do it online anytime before your appointment.
                        </p>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="bi bi-hospital"></i>
                        </div>
                        <h3 class="text-center mb-3">Multiple Clinics</h3>
                        <p class="text-center text-muted">
                            Choose from our network of clinics across different locations.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- How It Works Section -->
    <section class="info-section">
        <div class="container">
            <h2 class="section-title">How It Works</h2>

            <div class="row align-items-center g-5">
                <div class="col-md-3 text-center">
                    <div class="info-icon mb-3">
                        <i class="bi bi-1-circle-fill"></i>
                    </div>
                    <h4>Select Clinic</h4>
                    <p class="text-muted">Choose your preferred clinic location</p>
                </div>

                <div class="col-md-3 text-center">
                    <div class="info-icon mb-3">
                        <i class="bi bi-2-circle-fill"></i>
                    </div>
                    <h4>Pick Date & Time</h4>
                    <p class="text-muted">Select available date and time slot</p>
                </div>

                <div class="col-md-3 text-center">
                    <div class="info-icon mb-3">
                        <i class="bi bi-3-circle-fill"></i>
                    </div>
                    <h4>Enter Details</h4>
                    <p class="text-muted">Provide your name and phone number</p>
                </div>

                <div class="col-md-3 text-center">
                    <div class="info-icon mb-3">
                        <i class="bi bi-4-circle-fill"></i>
                    </div>
                    <h4>Confirm Booking</h4>
                    <p class="text-muted">You're all set! See you at the clinic</p>
                </div>
            </div>

            <div class="text-center mt-5">
                <a href="/book" class="btn btn-lg btn-primary" style="padding: 15px 50px; border-radius: 50px; font-weight: 600;">
                    <i class="bi bi-calendar-check"></i> Start Booking Now
                </a>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h5><i class="bi bi-heart-pulse"></i> RehabPlus System</h5>
                    <p class="mt-3">Professional Physiotherapy & Rehabilitation Services</p>
                    <p class="mt-4 mb-0">
                        <small>&copy; 2025 RehabPlus. All rights reserved.</small>
                    </p>
                    <p class="mt-2">
                        <a href="/login" class="text-white text-decoration-none">
                            <small><i class="bi bi-lock"></i> Staff Access</small>
                        </a>
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
</body>
</html>